import { ForwardPipePipe } from './forward-pipe.pipe';

describe('ForwardPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ForwardPipePipe();
    expect(pipe).toBeTruthy();
  });
});
