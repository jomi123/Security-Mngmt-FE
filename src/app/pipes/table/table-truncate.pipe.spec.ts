import { TableTruncatePipe } from './table-truncate.pipe';

describe('TableTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new TableTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
